void HeartRateStub(void)
{
    //Algorithm
    //(1). Configure the RB1 Interrupt
    //(2). Configure Timer0 to roll over every 2 seconds
    //(3). On every Timer0 overflow update the LCD
    //(4). After 5 Timer0 overflows c heck the value of count
    //(5). Print the value of count*6 to the LCD
}
void GlucoseStub(void){}
void SPO2Stub(void){}